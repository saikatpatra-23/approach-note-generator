# Copy this file to .env and fill in your values

ANTHROPIC_API_KEY=sk-ant-REPLACE_WITH_YOUR_KEY

# Password to access the app (leave blank to disable password gate)
APP_PASSWORD=TataTech2024

# Optional Postgres + pgvector connection string for the reusable KB
DATABASE_URL=postgresql+psycopg://username:password@localhost:5432/solution_copilot

# Optional local data directory override
DATA_DIR=.data
