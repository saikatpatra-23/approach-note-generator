# For Streamlit Cloud deployment — copy to secrets.toml (never commit secrets.toml!)
# In Streamlit Cloud dashboard, add these under Settings > Secrets

ANTHROPIC_API_KEY = "sk-ant-REPLACE_WITH_YOUR_KEY"
APP_PASSWORD = "TataTech2024"
